name: Docker Build & Push

on:
  push:
    branches:
      - master

jobs:
  push_to_dockerhub:
    name: Push Docker images
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: layp
          password: dckr_pat_O3gjK3Wu9faAYxX2tzlSP0JAdaU

      - name: Build and push customers image
        run: |
          docker build -t layp/customers:latest ./customers
          docker push layp/customers:latest

      - name: Build and push orders image
        run: |
          docker build -t layp/orders:latest ./orders
          docker push layp/orders:latest

      - name: Build and push products image
        run: |
          docker build -t layp/products:latest ./products
          docker push layp/products:latest
